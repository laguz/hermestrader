# HermesTrader Core Python dependencies

# Web / API
fastapi==0.136.1
uvicorn[standard]==0.47.0
watchfiles==1.2.0

# Database — Postgres/Timescale only (no SQLite fallback)
sqlalchemy==2.0.49
psycopg[binary]==3.3.4
pgvector==0.4.2   # doctrine-embedding vector column + cosine search
duckdb==1.5.3
alembic==1.13.3   # Postgres/Timescale schema migrations (see alembic/)

# Security
cryptography==48.0.0

# Core Data structures (shared with strategies/timeseries)
pandas==3.0.3
numpy==2.4.6

# HTTP + resilience
requests==2.34.2
tenacity==9.1.4

# LLM clients
ollama==0.6.2

# Timezone data — provides zoneinfo fallback when OS tzdata is absent
# (required for market_hours.py ZoneInfo("America/New_York"))
tzdata==2026.2

# MCP
mcp[cli]==1.27.1

# Modernization dependencies
pydantic-settings==2.14.1
GitPython==3.1.50
httpx==0.28.1
sse-starlette==3.4.4
transitions==0.9.3
greenlet==3.5.1
redis==5.0.8

