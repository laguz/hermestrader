The issue description had `self.tradier_service.get_historical_pricing` but `MLService` uses `self.tradier` (the constructor takes `tradier_service` but assigns it to `self.tradier = tradier_service`). Testing the code on `MLService` directly reveals that mocking `ml_service.tradier_service` fails with an AttributeError. The correct attribute to mock for `MLService` is `ml_service.tradier`.
